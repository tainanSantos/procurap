import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:procurap/app/modules/home/property/filter/filter_page.dart';

void main() {
  testWidgets('FilterPage has title', (tester) async {
  //  await tester.pumpWidget(buildTestableWidget(FilterPage(title: 'Filter')));
  //  final titleFinder = find.text('Filter');
  //  expect(titleFinder, findsOneWidget);
  });
}
  