import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:procurap/app/modules/home/property/list/list_page.dart';

void main() {
  testWidgets('ListPage has title', (tester) async {
  //  await tester.pumpWidget(buildTestableWidget(ListPage(title: 'List')));
  //  final titleFinder = find.text('List');
  //  expect(titleFinder, findsOneWidget);
  });
}
  