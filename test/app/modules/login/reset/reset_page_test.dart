import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:procurap/app/modules/login/reset/reset_page.dart';

void main() {
  testWidgets('ResetPage has title', (tester) async {
  //  await tester.pumpWidget(buildTestableWidget(ResetPage(title: 'Reset')));
  //  final titleFinder = find.text('Reset');
  //  expect(titleFinder, findsOneWidget);
  });
}
  